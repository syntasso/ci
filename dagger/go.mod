module dagger/ci

go 1.26

// Run `dagger develop` from this directory to pin the Dagger SDK version
// and generate dagger.gen.go before building or calling this module.
require dagger.io/dagger v0.18.0
